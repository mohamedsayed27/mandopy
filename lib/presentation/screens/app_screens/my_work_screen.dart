import 'package:flutter/material.dart';

class MyWorkScreen extends StatelessWidget {
  const MyWorkScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("MyWorkScreen")),
    );
  }
}
